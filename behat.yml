default:
    autoload:
        '': '%paths.base%/features/bootstrap'
    suites:
        acceptance:
            paths:
                - '%paths.base%/features'
            contexts:
                - Tests\Acceptance\FeatureContext
    formatters:
        pretty: true
